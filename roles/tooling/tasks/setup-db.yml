- name: Install PyMySQL
  pip:
    name: pymysql
    state: present
    
- name: create database
  community.mysql.mysql_db:
    login_host: "terraform-2022061622244065030000000c.cvqm1hu6ijyh.us-east-2.rds.amazonaws.com"
    login_user: "yheancarh"
    login_password: "dm!n1234"
    name: toolingdb
    collation: utf8_general_ci
    encoding: utf8
    state: present

- name: Input tooling credentials
  ansible.builtin.replace:
    path: /var/www/html/functions.php
    regexp: "{{ item.regexp }}"
    replace: "{{ item.line }}"
  loop:
    - { regexp: '^mysql.tooling.svc.cluster.local', line: "terraform-2022061622244065030000000c.cvqm1hu6ijyh.us-east-2.rds.amazonaws.com" }
    - { regexp: '^admin', line: "yheancarh" }
    - { regexp: '^tooling', line: "toolingdb" }
    - { regexp: '^admin', line: "dm!n1234" }


